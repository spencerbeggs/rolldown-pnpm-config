{
	"name": "@example/rolldown",
	"version": "0.0.0",
	"private": true,
	"description": "Vanilla rolldown example: the documented setup, as a runnable test",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/spencerbeggs/rolldown-pnpm-config.git",
		"directory": "examples/rolldown"
	},
	"license": "MIT",
	"author": {
		"name": "C. Spencer Beggs",
		"email": "spencer@beggs.codes",
		"url": "https://spencerbeg.gs"
	},
	"type": "module",
	"exports": {
		".": "./src/pnpmfile.ts",
		"./package.json": "./package.json"
	},
	"scripts": {
		"build:dev": "rolldown -c",
		"build:prod": "rolldown -c",
		"pnpm:export": "rolldown-pnpm-config export --dry-run",
		"pnpm:preview": "rolldown-pnpm-config preview",
		"pnpm:up": "rolldown-pnpm-config upgrade rolldown.config.ts --dry-run",
		"prepare": "turbo run build:dev",
		"types:check": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "catalog:silk",
		"rolldown": "catalog:silk",
		"rolldown-pnpm-config": "workspace:*",
		"typescript": "catalog:silk"
	},
	"engines": {
		"node": ">=24.11.0"
	},
	"publishConfig": {
		"directory": "dist/dev/pkg",
		"linkDirectory": true
	}
}
