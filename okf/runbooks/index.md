# Runbook

* [Build the dual outputs](build-the-dual-outputs.md) - Produce the dev and prod build outputs from `package/src/` via Turbo and `@savvy-web/bundler`.
* [Recover from a stale config-dependency lockfile](recover-from-a-stale-config-dependency-lockfile.md) - Force pnpm to pick up a `configDependencies` version bump when it keeps loading the old plugin.
* [Release and publish](release-and-publish.md) - The changeset-driven flow that versions `package/` and publishes it to the npm registry.
